{
  "name": "lango",
  "version": "0.1.0",
  "description": "Lango is an api gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "Mohamed Camara GUEYE",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "~3.1.0",
    "gts": "^0.9.0"
  },
  "dependencies": {
    "@types/express": "^4.16.0",
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "rxjs": "^6.3.3",
    "tslint-config-airbnb": "^5.11.1"
  }
}
